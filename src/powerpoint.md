Generate a PowerPoint (.pptx) presentation. Supports two authoring modes: a Python script (`python-pptx`) or structured YAML (a theme plus a list of slides).

Before calling, consult the **powerpoint** skill for the authoring format and rules. Use `fetch_uploaded_chat_file_ids` to get the IDs of any images uploaded in the chat that should be embedded.
