Generate a PDF document from a Python script (`reportlab`) — titles, paragraphs, tables, and images.

Before calling, consult the **pdf** skill for the script template and rules. Use `fetch_uploaded_chat_file_ids` to get the IDs of any images uploaded in the chat that should be embedded.
